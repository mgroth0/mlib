package:
  name: mlib-mgroth0
  version: 0.0.25

source:
  path: mlib
#  git_rev:
#  git_url:
build:
  number: 0
  script: "$HOME""/miniconda3/envs/build/bin/python setup.py sdist bdist_wheel"

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  imports:
    - mlib

#about:
#  home: