package:
  name: mlib-mgroth0
  version: 0.0.24

#source:
#  git_rev:
#  git_url:
#build:
#  number: 0
#  script: "python -m pip install . --no-deps --ignore-installed -vv "

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  imports:
    - mlib

#about:
#  home: